import 'package:flame/components.dart';

class PlayerLife extends SpriteComponent {
  PlayerLife({
    Sprite? animation,
    Vector2? size,
    Vector2? position,
  }) : super(sprite: animation, size: size, position: position);
}
